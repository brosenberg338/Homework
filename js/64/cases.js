function stringCaseInsensitiveEquals (day) {
  'use strict';
      for ( let i=0 ; i<myapp.views.days.length ; i++) {
      
  if (day.toUpperCase()=== myapp.views.days[i].toUpperCase) {
      return true;
  }
}}